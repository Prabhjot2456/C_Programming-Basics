#include<stdio.h>
int main(){
    int a;
    char b;
    float c;
    double d;
    printf("Size of int = %d",(int)sizeof(a));
    printf("\nSize of char = %d",(int)sizeof(b));
    printf("\nSize of float = %d",(int)sizeof(c));
    printf("\nSize of double = %d",(int)sizeof(d));
}